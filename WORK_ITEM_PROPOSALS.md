# 코드베이스 점검 후 제안 작업 4건

아래 항목은 현재 코드베이스를 빠르게 점검하며 확인한 리스크/개선 포인트를 기준으로, 요청하신 카테고리(오탈자, 버그 수정, 코드 댓글·문서 불일치, 테스트 개선)에 맞춰 각각 1건씩 제안한 것입니다.

## 1) 오탈자 수정 작업
**제안:** `main.py`의 단계 주석 번호를 정리합니다. 현재 `# 6.`이 두 번 반복되어 있어(분석 단계와 텔레그램 단계) 실행 흐름 파악 시 혼동을 줍니다. 두 번째 `# 6.`을 `# 7.`로, 기존 `# 7.`을 `# 8.`로 맞추는 소규모 정리를 권장합니다.

- 근거: 단계 주석이 중복되어 읽기 흐름이 어긋나 있습니다.
- 기대 효과: 온보딩/유지보수 시 실행 단계 이해도 향상.

## 2) 버그 수정 작업
**제안:** `state['seen_ids']` 키를 `article.id` 단독값에서 `"{board_id}:{article.id}"` 형태의 복합 키로 변경합니다.

- 근거: 현재는 `filter_new_articles()`와 `mark_as_seen()`가 공지 고유 식별자로 `id`만 사용하고 있습니다. 게시판이 다른데 숫자 ID가 우연히 겹치면, 아직 보지 않은 공지가 이미 본 것으로 처리될 가능성이 있습니다.
- 기대 효과: 게시판 간 ID 충돌로 인한 신규 공지 누락 방지.

## 3) 코드 댓글/문서 불일치 수정 작업
**제안:** `main.py`의 주석에서 `Gemini 관련도 분석`을 `Gemini 분석(실패 시 키워드 폴백)`으로 명시하고, 실행 로그에도 동일한 의미를 맞춰 문맥을 일치시킵니다.

- 근거: 실제 `match_articles()`는 Gemini 실패 시 `keyword_fallback()`으로 대체되는데, 메인 흐름 주석/로그는 Gemini만 사용하는 것처럼 읽힙니다.
- 기대 효과: 코드 독해 시 실제 동작과 설명의 일치성 개선.

## 4) 테스트 개선 작업
**제안:** 최소 단위 테스트를 추가합니다 (예: `pytest`). 우선순위는 아래 3개를 권장합니다.

1. `feeds.extract_article_id()` — 정상 URL/비정상 URL 케이스.
2. `matcher.keyword_fallback()` — high/medium 키워드 점수 부여 및 사유 문자열 검증.
3. `notifier.split_message()` — 4096자 경계값 및 줄 단위 분할 보장 검증.

- 근거: 현재 저장소에 테스트 코드가 없어 회귀 검출이 어렵습니다.
- 기대 효과: 공지 파싱, 분류, 알림 핵심 경로의 안정성 확보.
